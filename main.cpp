#include <iostream>
#include <Windows.h>
#include <ctime>
#include "CTetris.h"
using namespace std;

int main()
{
	// 1. test ~ !! 10 : 46
	//CTetris* bg = new CTetris;
	//bg->SetBlock();
	//int tick = 0;

	//while (1)
	//{
	//	if (GetAsyncKeyState(VK_UP) & 0x8001)
	//		bg->Spin();

	//	if (GetAsyncKeyState(VK_LEFT) & 0x8001)
	//		bg->MoveBlock(2);

	//	if (GetAsyncKeyState(VK_RIGHT) & 0x8001)
	//		bg->MoveBlock(3);

	//	if (GetAsyncKeyState(VK_DOWN) & 0x8001)
	//		bg->MoveBlock(1);

	//	tick++;
	//	if (tick % 10 == 0)
	//		bg->MoveBlock(1);

	//	bg->PrintMap();
	//	Sleep(100);
	//}

	//delete(bg);

	return 0;
}

